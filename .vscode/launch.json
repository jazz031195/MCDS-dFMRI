

{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "run current conf file",
            "type": "cppdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": ["${file}"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "MIMode": "lldb",
            "console": "integratedTerminal" 
        },
    ]
}